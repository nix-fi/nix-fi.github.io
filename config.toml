# The URL the site will be built for
base_url = "https://fi.nix.ug"

theme = "papermod"

# Whether to automatically compile all Sass files in the sass directory
# compile_sass = true

# Whether to build a search index to be used later on by a JavaScript library
build_search_index = true
taxonomies = [
       {name = "tags", feed = true},
]

[search]
# Whether to include the title of the page/section in the index
include_title = true
# Whether to include the description of the page/section in the index
include_description = false
# Whether to include the path of the page/section in the index
include_path = false
# Whether to include the rendered content of the page/section in the index
include_content = true
# At which character to truncate the content to. Useful if you have a lot of pages and the index would
# become too big to load on the site. Defaults to not being set.
# truncate_content_length = 100
# Wether to produce the search index as a javascript file or as a JSON file
# Accepted value "elasticlunr_javascript" or "elasticlunr_json"
index_format = "elasticlunr_json"

[markdown]
# Whether to do syntax highlighting
# Theme can be customised by setting the `highlight_theme` variable to a theme supported by Zola
highlight_code = true
render_emoji = true

[extra]
# Put all your custom variables here


[extra.papermod]
title = "Nix Users Finland"
default_theme = "auto"
theme = "dark"
language_direction = "auto"
keywords = []
show_theme_toggle = true
show_scroll_to_top = true
show_code_copy_buttons = true
show_reading_time = true
show_word_count = false
show_post_nav_links = true
show_post_meta = true
default_toc_open = false
show_post_edit_button = false
edit_post_url = "https://github.com/cydave/zola-theme-papermod/tree/master"
#copyright = ""
date_format = "%Y-%m-%d"
navigation = [
    { url = "$BASE_URL/meetups", title = "Meetups" },
    { url = "$BASE_URL/search", title = "Search" },
]

home_title = ""
icon = "/favicon.png"

home_content = '''
A Nix User Group and a venue for Functional Package Management enthusiasts in Finland.
'''
social_icons = [
    { url = "https://matrix.to/#/%23nix-hel%3Asomeonex.net?via=someonex.net", title = "Matrix", icon = "matrix" },
    { url = "https://t.me/+xSzubrnQz-w4OWE8", title = "Telegram", icon = "telegram" },
    { url = "https://github.com/nix-fi", title = "GitHub", icon = "github" },
]
